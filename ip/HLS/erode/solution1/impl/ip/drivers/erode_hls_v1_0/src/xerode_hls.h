// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef XERODE_HLS_H
#define XERODE_HLS_H

#ifdef __cplusplus
extern "C" {
#endif

/***************************** Include Files *********************************/
#ifndef __linux__
#include "xil_types.h"
#include "xil_assert.h"
#include "xstatus.h"
#include "xil_io.h"
#else
#include <stdint.h>
#include <assert.h>
#include <dirent.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>
#include <unistd.h>
#include <stddef.h>
#endif
#include "xerode_hls_hw.h"

/**************************** Type Definitions ******************************/
#ifdef __linux__
typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;
#else
typedef struct {
    u16 DeviceId;
    u32 Control_bus_BaseAddress;
} XErode_hls_Config;
#endif

typedef struct {
    u32 Control_bus_BaseAddress;
    u32 IsReady;
} XErode_hls;

/***************** Macros (Inline Functions) Definitions *********************/
#ifndef __linux__
#define XErode_hls_WriteReg(BaseAddress, RegOffset, Data) \
    Xil_Out32((BaseAddress) + (RegOffset), (u32)(Data))
#define XErode_hls_ReadReg(BaseAddress, RegOffset) \
    Xil_In32((BaseAddress) + (RegOffset))
#else
#define XErode_hls_WriteReg(BaseAddress, RegOffset, Data) \
    *(volatile u32*)((BaseAddress) + (RegOffset)) = (u32)(Data)
#define XErode_hls_ReadReg(BaseAddress, RegOffset) \
    *(volatile u32*)((BaseAddress) + (RegOffset))

#define Xil_AssertVoid(expr)    assert(expr)
#define Xil_AssertNonvoid(expr) assert(expr)

#define XST_SUCCESS             0
#define XST_DEVICE_NOT_FOUND    2
#define XST_OPEN_DEVICE_FAILED  3
#define XIL_COMPONENT_IS_READY  1
#endif

/************************** Function Prototypes *****************************/
#ifndef __linux__
int XErode_hls_Initialize(XErode_hls *InstancePtr, u16 DeviceId);
XErode_hls_Config* XErode_hls_LookupConfig(u16 DeviceId);
int XErode_hls_CfgInitialize(XErode_hls *InstancePtr, XErode_hls_Config *ConfigPtr);
#else
int XErode_hls_Initialize(XErode_hls *InstancePtr, const char* InstanceName);
int XErode_hls_Release(XErode_hls *InstancePtr);
#endif

void XErode_hls_Start(XErode_hls *InstancePtr);
u32 XErode_hls_IsDone(XErode_hls *InstancePtr);
u32 XErode_hls_IsIdle(XErode_hls *InstancePtr);
u32 XErode_hls_IsReady(XErode_hls *InstancePtr);
void XErode_hls_EnableAutoRestart(XErode_hls *InstancePtr);
void XErode_hls_DisableAutoRestart(XErode_hls *InstancePtr);

void XErode_hls_Set_rows(XErode_hls *InstancePtr, u32 Data);
u32 XErode_hls_Get_rows(XErode_hls *InstancePtr);
void XErode_hls_Set_cols(XErode_hls *InstancePtr, u32 Data);
u32 XErode_hls_Get_cols(XErode_hls *InstancePtr);
void XErode_hls_Set_channels(XErode_hls *InstancePtr, u32 Data);
u32 XErode_hls_Get_channels(XErode_hls *InstancePtr);
void XErode_hls_Set_mode(XErode_hls *InstancePtr, u32 Data);
u32 XErode_hls_Get_mode(XErode_hls *InstancePtr);

void XErode_hls_InterruptGlobalEnable(XErode_hls *InstancePtr);
void XErode_hls_InterruptGlobalDisable(XErode_hls *InstancePtr);
void XErode_hls_InterruptEnable(XErode_hls *InstancePtr, u32 Mask);
void XErode_hls_InterruptDisable(XErode_hls *InstancePtr, u32 Mask);
void XErode_hls_InterruptClear(XErode_hls *InstancePtr, u32 Mask);
u32 XErode_hls_InterruptGetEnabled(XErode_hls *InstancePtr);
u32 XErode_hls_InterruptGetStatus(XErode_hls *InstancePtr);

#ifdef __cplusplus
}
#endif

#endif
